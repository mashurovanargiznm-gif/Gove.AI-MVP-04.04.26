Ты — Senior AI & Backend Architect. У меня уже готов интерфейс и базовая структура проекта. Твоя задача — написать ТОЛЬКО изолированный логический модуль ядра `gove_engine.py` для финтех-проекта.

Этот модуль должен демонстрировать интеллектуальную логику работы с должниками: динамическое удержание (на основе ОКЭД) и фильтрацию транзакций (на основе КНП).

НАПИШИ КОД, КОТОРЫЙ РЕАЛИЗУЕТ СЛЕДУЮЩИЕ ПРАВИЛА:

1. Справочник ОКЭД и Маржинальности (Смоделируй базу знаний):
Создай словарь или класс, который мапит коды ОКЭД на уровень маржинальности и процент удержания для ЧСИ:
- Низкая маржа (например, ОКЭД 47111 - Розничная торговля, супермаркеты): 5%
- Средняя маржа (например, ОКЭД 10710 - Производство хлеба, услуги): 12%
- Высокая маржа (например, ОКЭД 62011 - Разработка ПО, IT, финансы): 15%

2. Функция `calculate_dynamic_retention(amount, oked_code)`:
- Принимает сумму поступления и ОКЭД компании.
- Определяет процент удержания.
- Возвращает словарь: { "gross_amount": ..., "chsi_withheld": ..., "net_to_business": ..., "margin_tier": ..., "percentage_applied": ... }

3. Функция `evaluate_transaction(amount, knp_code, purpose, company_strikes)`:
- Если КНП 110 (Зарплата) или 900+ (Налоги): статус "Разрешено" (зеленый свет).
- Если транзакция подозрительная (например, КНП 850 или 421) и сумма не бьется с историей (пропиши простую моковую логику проверки): статус "Серая зона" (требует ЧСИ).
- Если у компании уже есть 3 страйка: статус "Заблокировано" (Critical Risk).
- Функция должна возвращать статус, цвет индикатора для фронтенда и текстовое обоснование от ИИ.

4. БЛОК ДЕМОНСТРАЦИИ (ОЧЕНЬ ВАЖНО):
В конце файла напиши конструкцию `if __name__ == "__main__":`, которая прогоняет эффектную симуляцию для терминала. Выведи в консоль красивый лог (используй ASCII форматирование, чтобы это выглядело как терминал серьезной системы):
- Поступление 5 000 000 тг на счет Супермаркета (ОКЭД 47111). Покажи, что система поняла "Низкая маржа" и удержала 5% (250 000 тг).
- Поступление 5 000 000 тг на счет IT-компании (ОКЭД 62011). Покажи удержание 15% (750 000 тг).
- Попытка перевода зарплаты (КНП 110) — одобрено.
- Попытка сомнительного перевода дивидендов — серая зона.

Код должен быть готов к импорту (без лишних веб-фреймворков, чистый Python), с аннотациями типов (Type Hints) и подробными комментариями.